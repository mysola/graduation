<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wangyang.mapper.RealNameUrlClickDao">

    <select id="getUrlClick" resultType="RealNameUrlClick">
        select doc_url.url as url , doc_click.click_count as realNameClick,
        user_inf.username as username from
        doc_url,user_inf,doc_click where username = #{username} and url = #{url}
    </select>
</mapper>